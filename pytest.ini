[pytest]
# autobot test suite — isolated from the B20/web3 environment
# Tells pytest to collect only from autobot/tests
testpaths = autobot/tests
# Disable broken web3 pytest plugin (eth_typing version mismatch in this env)
addopts = -p no:pytest_ethereum
